#Python Code Challenge: Lists_1

def append_size(lst):
   lst.append(len(lst))
   return lst

#Uncomment the line below when your function is done
print(append_size([23, 42, 108]))

#At first I thought I needed to define the list before defining
the function, but apparently it is sufficient to define the list inside
the print function